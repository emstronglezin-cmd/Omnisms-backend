{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "OmniSMS API",
    "description": "Documentation de l'API pour OmniSMS."
  },
  "host": "localhost:5000",
  "basePath": "/",
  "schemes": ["http"],
  "paths": {
    "/auth/register": {
      "post": {
        "summary": "Inscription utilisateur",
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/Register"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Utilisateur inscrit avec succès."
          }
        }
      }
    }
  },
  "definitions": {
    "Register": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "country": { "type": "string" },
        "phone": { "type": "string" },
        "password": { "type": "string" }
      }
    }
  }
}